# 確認用ローカルサーバー設定（Claude Codeのプレビュー用・サイト本体ではない）
.claude/

# OS/エディタが勝手に作るファイル
Thumbs.db
desktop.ini
.DS_Store

# 印刷解像度の元画像（Web用に縮小したものが img/art/ にある。原本はGoogleドライブ）
_assets-original/

# 帯の再生成スクリプト用（tools/build-strip.mjs で使う sharp）
node_modules/
package-lock.json

# 確認用の単体HTML（tools/build-preview.mjs が生成。本番にはアップしない）
preview-standalone.html
