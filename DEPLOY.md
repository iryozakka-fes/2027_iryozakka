# 公開手順（2027ティザー）

Xserver の `iryozakka.com` にティザーを出すまでの手順。
ファイルマネージャ（Xserverのサーバーパネル内）だけで完結する。FTPソフトは要らない。

---

## ⚠️ 先に読む：触ってはいけないフォルダ

`public_html` の中には、**2026サイトとは無関係なのに同じ場所に置かれているもの**がある。
引っ越しのときに巻き込んで移すと、動いているものが壊れる。

| フォルダ | 正体 | 扱い |
| --- | --- | --- |
| `2025/` | **2025.iryozakka.com の実体**。サブドメインがこのフォルダを指している | **動かさない** |
| `members/` | **Basic認証つきのメンバー用**（ゲート表などが入っている） | **動かさない** |
| `img2027/` | 2027ティザーの画像 | **動かさない** |

`2025/` を移すと去年のサイトが落ちる。`members/` を移すとメンバーがゲート表を見られなくなる。

---

## 現状（2026/9/9 時点・外からHTTPで確認した範囲）

```
public_html/
├── index.html                2026サイトのトップ
├── css/                      2026のCSS
├── js/                       2026のJS
├── img/                      2026の画像
├── 2025/                     ← 2025サイト。動かさない
├── 2026.iryozakka.com/       ← 2026サブドメインの中身。いまは空
└── members/                  ← 認証つき。動かさない
```

> **要確認**：この一覧は外からURLを叩いて分かった範囲。
> indexが無く一覧も禁止されているフォルダは検出できないので、
> **実際のファイルマネージャで見たものが正**。`.htaccess` の有無も外からは見えない。

### 2026サブドメインのフォルダ名に注意

`2026.iryozakka.com` はサブドメイン作成済み・DNS反映済みで、Xserverの初期ページが出ている（＝フォルダは空）。
**引っ越し先のフォルダは `public_html/2026.iryozakka.com/`**（サブドメイン名がそのままフォルダ名）。

2025とは流儀が違うので混同しないこと。

| サブドメイン | 実際のフォルダ |
| --- | --- |
| `2025.iryozakka.com` | `public_html/2025/` （短い形） |
| `2026.iryozakka.com` | `public_html/2026.iryozakka.com/` （フルの形） |

Xserverのヘルプにはどちらの形も載っていて、作った時期によって変わる。
`public_html/2026/` は**存在しない**（404で確認済み）ので、そこを探しても見つからない。

---

## 第1段階：トップをティザーに差し替える

2026サイトのファイルは**動かさない**。トップに出るものだけを入れ替える。

### 1. 今のトップを退避する

ファイルマネージャで `public_html/index.html` を **`index-2026.html`** にリネームする。

これで2026サイトは `https://iryozakka.com/index-2026.html` で見られる状態のまま残る。
CSS・JS・画像は同じ場所にあるので、リンクは切れない。

### 2. ティザーをアップロードする

このリポジトリから、**この2つだけ**を `public_html/` の直下にアップロードする。

```
index.html
img2027/        ← 中の art/ と icon/ ごと
```

`tools/` と `_assets-original/` は**アップロードしない**（前者は帯の画像を作り直すためのスクリプト、後者は印刷用の原本）。

アップロード後の状態：

```
public_html/
├── index.html        ← 2027ティザー
├── index-2026.html   ← 2026サイト（退避済み）
├── img2027/          ← 2027の画像
├── css/ js/ img/     ← 2026のまま
├── 2025/  members/   ← 触っていない
```

### 3. 確認する

- `https://iryozakka.com/` → ティザーが出る
- `https://iryozakka.com/index-2026.html` → 2026サイトが崩れずに出る
- `https://2025.iryozakka.com/` → 今まで通り出る
- スマホでも開いて、横スクロールが出ないか見る

### 戻したいとき

`index.html` を別名にして、`index-2026.html` を `index.html` に戻すだけ。
2026のファイルは何も動かしていないので、これで完全に元通りになる。

---

## 第2段階：2026サイトをサブドメインへ引っ越す（後日でよい）

第1段階が終わっていれば急がない。落ち着いてから。

### 移すもの

- `index-2026.html`（移した先で `index.html` に戻す）
- `css/`
- `js/`
- `img/`

### 移さないもの

- `2025/` `members/` `img2027/` `index.html`（＝ティザー）

### 手順

1. `public_html/2026.iryozakka.com/` を開く（このフォルダが引っ越し先）
2. そこに、上の4つをコピーする（**移動ではなくコピー**）
3. コピー先で `index-2026.html` を `index.html` にリネームする
   - 元からあるXserverの初期ページ（`index.html` や `default_page.png`）は消してよい
4. `https://2026.iryozakka.com/` を開いて、崩れずに出るか確認する
5. **問題なければ**、`public_html/` 側の `index-2026.html` `css/` `js/` `img/` を消す

> 3で名前を戻し忘れると、サブドメインを開いてもXserverの初期ページのままになる。
> 5は「先にコピーして、動作確認してから消す」の順を守ること。先に消すと戻せない。

---

## メモ

### og:image

SNSでシェアされたときに出る画像。`index.html` の `<head>` にある。

```html
<meta property="og:image" content="https://iryozakka.com/img2027/logo-2027.png">
```

ティザーを `iryozakka.com` のトップに置く前提のURL。別の場所に置くならここを直す。

### アクセス解析

未設置。2026サイトは Google Analytics（`G-BKSBYPVXEP`）を入れていた。
アクセス数を取るなら、そのタグを `<head>` に貼る。貼る場所にコメントを入れてある。

### アイコン帯を作り直すとき

上下の帯は `img2027/icon/strip.png` を1枚の画像として横に繰り返している。
アイコンを足す・外す・並べ替えたときは、画像を作り直す必要がある。

```
npm install sharp        （初回のみ）
node tools/build-strip.mjs
```

並び順・間隔は `tools/build-strip.mjs` の `ORDER` と `GAP` で変えられる。

### Webフォント

日付の丸ゴシックは Google Fonts の **M PLUS Rounded 1c**。
指定のあった Rounded M+ 2p は Google Fonts に無いため、同じM+系のこちらを使っている。
厳密に 2p にしたい場合は、フォントファイルを自前で置いて `@font-face` で差し替える。
